# QA-001 - Regressão e integração

## Status
`AUTO_VALIDATED`

## Owner / Workstream
`TESTS_QA`

## Objetivo
Executar somente esta entrega, respeitando `AGENTS.md` e `PLAN.md`.

## DEPENDS_ON
`Features-alvo em REVIEW`

## BLOCKS
Consultar `PLAN.md`.

## Arquivos permitidos
- `docs/tasks/QA-001.md`
- `PROJECT_STATE.md`
- `PLAN.md`

## Arquivos proibidos
- código de produto.
- migrations.
- prompts finais.
- estilos finais.
- preços ou gateway real.

## Shared files / lock required
`LOCK_REQUIRED: false`

## Critérios de aceite
- Git limpo antes da auditoria final.
- `python manage.py check` passa.
- `python manage.py makemigrations --check --dry-run` não detecta pendências.
- Suíte Django passa.
- `npm run lint` passa.
- `npm run build` passa.
- Documentação central sincronizada.

## Testes obrigatórios
```bash
python manage.py check
python manage.py makemigrations --check --dry-run
python manage.py test --keepdb
npm run lint
npm run build
```

## Relatório final
### Arquivos alterados

- `docs/tasks/QA-001.md`
- `PROJECT_STATE.md`
- `PLAN.md`

### Validações executadas

- `git status --short`: limpo antes da auditoria final.
- `python manage.py check`: passou.
- `python manage.py makemigrations --check --dry-run`: passou.
- `python manage.py test --keepdb`: passou, 40 testes.
- `npm run lint`: passou.
- `npm run build`: passou.

### Resultado

- Marco `STRUCTURAL_READY` atingido localmente na branch `agent/STRUCTURAL-READY`.
- Infraestrutura estrutural validada para Foundation, Billing, Credits, SuperAdmin, AI, Studio, PromptEngine, APIs e frontend.

### Riscos ou pendências

- Push remoto bloqueado por credenciais locais do Git.
- Validação visual autenticada com upload/geração real ainda exige ação humana.
- Prompts finais, estilos finais, preços definitivos e gateway real permanecem fora de `STRUCTURAL_READY`.

### Decisões que exigem validação humana

- Aprovação visual dos modos.
- Definição final de cenários, prompts e estilos comerciais.
- Validação de pagamento real quando o gateway for escolhido.
